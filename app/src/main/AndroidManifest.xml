<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:versionName="@string/app_version"
    package="com.sakhhome.vehicle">

    <application
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyVehicle">
        <activity android:name="com.sakhhome.vehicle.activities.MainAppActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="com.sakhhome.vehicle.activities.SelectVehicleActivity" />
        <activity android:name="com.sakhhome.vehicle.activities.OilChangeActivity" />
        <activity android:name="com.sakhhome.vehicle.activities.RefuelingActivity" />
        <activity android:name="com.sakhhome.vehicle.activities.AboutActivity" />
        <activity android:name="com.sakhhome.vehicle.activities.SettingActivity" />
        <activity android:name="com.sakhhome.vehicle.activities.OsagoActivity" />
    </application>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
</manifest>